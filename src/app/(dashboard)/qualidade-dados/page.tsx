import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function Page() {
  return (
    <PagePlaceholder
      title="Qualidade dos dados"
      description="Pedidos duplicados, campos ausentes e inconsistências."
      phase="Fase 2 — Pedidos e importações"
    />
  );
}
